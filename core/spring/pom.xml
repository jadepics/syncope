<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.apache.syncope</groupId>
    <artifactId>syncope-core</artifactId>
    <version>5.0.0-SNAPSHOT</version>
  </parent>

  <name>Apache Syncope Core Spring</name>
  <description>Apache Syncope Core Misc</description>
  <groupId>org.apache.syncope.core</groupId>
  <artifactId>syncope-core-spring</artifactId>
  <packaging>jar</packaging>
  
  <properties>
    <rootpom.basedir>${basedir}/../..</rootpom.basedir>
  </properties>

  <dependencies>    
    <dependency> 
      <groupId>jakarta.servlet</groupId> 
      <artifactId>jakarta.servlet-api</artifactId> 
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>javax.cache</groupId>
      <artifactId>cache-api</artifactId>
    </dependency>

    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-text</artifactId>
    </dependency>

    <dependency>
      <groupId>org.jasypt</groupId>
      <artifactId>jasypt</artifactId>
    </dependency>

    <dependency>
      <groupId>com.nimbusds</groupId>
      <artifactId>nimbus-jose-jwt</artifactId>
    </dependency>

    <dependency>
      <groupId>org.passay</groupId>
      <artifactId>passay</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
      <exclusions>
        <exclusion>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-starter-logging</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>org.aspectj</groupId>
      <artifactId>aspectjweaver</artifactId>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.uuid</groupId>
      <artifactId>java-uuid-generator</artifactId>
    </dependency>

    <dependency>
      <groupId>dev.samstevens.totp</groupId>
      <artifactId>totp</artifactId>
    </dependency>
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>core</artifactId>
    </dependency>
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>javase</artifactId>
    </dependency>

    <dependency>
      <groupId>org.apache.syncope.core</groupId>
      <artifactId>syncope-core-provisioning-api</artifactId>
      <version>${project.version}</version>
    </dependency>        
    <dependency>
      <groupId>org.apache.syncope.common.idm</groupId>
      <artifactId>syncope-common-idm-rest-api</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.syncope.common.am</groupId>
      <artifactId>syncope-common-am-rest-api</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.syncope.common.keymaster</groupId>
      <artifactId>syncope-common-keymaster-client-api</artifactId>
      <version>${project.version}</version>
    </dependency>
    
    <!-- TEST -->
    <dependency>
      <groupId>com.github.ben-manes.caffeine</groupId>
      <artifactId>jcache</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
      </plugin>
    </plugins>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
        <includes>
          <include>**/security.properties</include>
        </includes>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>false</filtering>
        <excludes>
          <exclude>**/security.properties</exclude>
        </excludes>
      </resource>
    </resources>
  </build>
  <profiles>
    <profile>
      <id>pit-access-token-verifier</id>


      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>1.25.8</version>

            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>1.2.3</version>
              </dependency>
            </dependencies>

            <configuration>
              <targetClasses>
                <param>org.apache.syncope.core.spring.security.jws.AccessTokenJWSVerifier*</param>
              </targetClasses>

              <targetTests>
                <param>org.apache.syncope.core.spring.security.jws.AccessTokenJWS*</param>
              </targetTests>

              <mutators>
                <mutator>DEFAULTS</mutator>
              </mutators>

              <threads>2</threads>

              <outputFormats>
                <param>HTML</param>
                <param>XML</param>
                <param>CSV</param>
              </outputFormats>

              <reportsDirectory>${project.build.directory}/pit-reports/access-token-verifier</reportsDirectory>

              <verbose>true</verbose>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>pit-access-token-verifier-extended</id>

      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>1.25.9</version>

            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>1.2.3</version>
              </dependency>
            </dependencies>

            <configuration>
              <targetClasses>
                <param>
                  org.apache.syncope.core.spring.security.jws.AccessTokenJWSVerifier
                </param>
              </targetClasses>

              <targetTests>
                <param>
                  org.apache.syncope.core.spring.security.jws.AccessTokenJWSVerifier*Test
                </param>
              </targetTests>

              <mutators>
                <!-- Mutatori standard più rimozione delle condizioni verso else. -->
                <mutator>STRONGER</mutator>

                <!-- Complementare: forza l'esecuzione del ramo if. -->
                <mutator>REMOVE_CONDITIONALS_EQUAL_IF</mutator>

                <!-- Elimina chiamate non-void sostituendone il risultato. -->
                <mutator>NON_VOID_METHOD_CALLS</mutator>

                <!-- Modifica le costanti inline presenti nel bytecode. -->
                <mutator>INLINE_CONSTS</mutator>

                <!-- Sostituisce con null i risultati dei costruttori. -->
                <mutator>CONSTRUCTOR_CALLS</mutator>
              </mutators>

              <outputFormats>
                <param>HTML</param>
                <param>XML</param>
              </outputFormats>

              <reportsDirectory>
                ${project.build.directory}/pit-reports/access-token-verifier-extended
              </reportsDirectory>

              <timestampedReports>false</timestampedReports>
              <threads>1</threads>
              <verbose>false</verbose>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>pit-access-token-verifier-integration-only</id>

      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>1.25.9</version>

            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>1.2.3</version>
              </dependency>
            </dependencies>

            <configuration>

              <targetClasses>
                <param>
                  org.apache.syncope.core.spring.security.jws.AccessTokenJWSVerifier
                </param>
              </targetClasses>

              <targetTests>
                <param>
                  org.apache.syncope.core.spring.security.jws.AccessTokenJWSSignerVerifierIntegrationTest
                </param>
              </targetTests>

              <mutators>
                <mutator>STRONGER</mutator>
                <mutator>REMOVE_CONDITIONALS_EQUAL_IF</mutator>
                <mutator>NON_VOID_METHOD_CALLS</mutator>
                <mutator>INLINE_CONSTS</mutator>
                <mutator>CONSTRUCTOR_CALLS</mutator>
              </mutators>

              <outputFormats>
                <param>HTML</param>
                <param>XML</param>
              </outputFormats>

              <reportsDirectory>
                ${project.build.directory}/pit-reports/access-token-verifier-integration-only
              </reportsDirectory>

              <timestampedReports>false</timestampedReports>
              <threads>1</threads>

            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>pit-access-token-jws-integration</id>

      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>1.25.9</version>

            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>1.2.3</version>
              </dependency>
            </dependencies>

            <configuration>

              <!--
                  In questa campagna vengono mutate entrambe
                  le classi coinvolte nell'integrazione.
              -->
              <targetClasses>
                <param>
                  org.apache.syncope.core.spring.security.jws.AccessTokenJWSVerifier
                </param>

                <param>
                  org.apache.syncope.core.spring.security.jws.AccessTokenJWSSigner
                </param>
              </targetClasses>

              <!--
                  Viene utilizzato esclusivamente il test
                  d'integrazione Signer -> Verifier.
              -->
              <targetTests>
                <param>
                  org.apache.syncope.core.spring.security.jws.AccessTokenJWSSignerVerifierIntegrationTest
                </param>
                <param>
                  org.apache.syncope.core.spring.security.jws.AccessTokenJWSSignerVerifierIntegrationBlackBoxRefinementTest
                </param>

              </targetTests>

              <!--
                  Manteniamo lo stesso insieme esteso già
                  utilizzato sul Verifier, così i risultati
                  rimangono metodologicamente confrontabili.
              -->
              <mutators>
                <mutator>STRONGER</mutator>
                <mutator>REMOVE_CONDITIONALS_EQUAL_IF</mutator>
                <mutator>NON_VOID_METHOD_CALLS</mutator>
                <mutator>INLINE_CONSTS</mutator>
                <mutator>CONSTRUCTOR_CALLS</mutator>
              </mutators>

              <outputFormats>
                <param>HTML</param>
                <param>XML</param>
              </outputFormats>

              <reportsDirectory>
                ${project.build.directory}/pit-reports/access-token-jws-integration
              </reportsDirectory>

              <timestampedReports>false</timestampedReports>

              <threads>1</threads>

            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>randoop-only</id>

      <dependencies>

        <!--
          I test prodotti da Randoop 4.3.4 sono JUnit 4.
        -->
        <dependency>
          <groupId>junit</groupId>
          <artifactId>junit</artifactId>
          <version>4.13.2</version>
          <scope>test</scope>
        </dependency>

        <!--
          Permette alla piattaforma JUnit già usata da Syncope
          di eseguire anche i test JUnit 4 generati da Randoop.
        -->
        <dependency>
          <groupId>org.junit.vintage</groupId>
          <artifactId>junit-vintage-engine</artifactId>
          <scope>test</scope>
        </dependency>

      </dependencies>

      <build>
        <plugins>

          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>1.25.9</version>

            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>1.2.3</version>
              </dependency>
            </dependencies>

          </plugin>

        </plugins>
      </build>
    </profile>
  </profiles>
</project>
